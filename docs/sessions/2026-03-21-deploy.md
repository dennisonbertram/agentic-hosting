# 2026-03-21 Deploy + Rename: paasd -> ah

## Objective

Deploy latest code (localhost routing fixes, health check updates, new migrations) and cut over from `paasd` naming to `ah` on server `65.21.67.254`.

## Results: All 20 Steps Passed

### Pre-deploy: Sync and Build

| Step | Action | Result |
|------|--------|--------|
| 1 | Check paasd status | `active (running)` since 2026-03-19 15:29:26 |
| 2 | rsync source to server | 562 KB sent, all files synced |
| 3 | Build binary on server (CGO) | Success, `bin/ah` built |

### Cutover

| Step | Action | Result |
|------|--------|--------|
| 4 | Copy env file `/etc/default/paasd` -> `/etc/default/ah` | Done |
| 5 | Copy binary to `/usr/local/bin/ah` | Done |
| 6 | Stop paasd.service (downtime starts) | Stopped |
| 7 | Copy live databases (state + metering) | Copied (WAL/SHM included) |
| 8 | Copy builds/ and backups/ | Done |
| 9 | Copy registry (replace symlink with real copy) | Done |
| 10 | Start ah.service (downtime ends) | Started, PID 1698590 |
| 11 | Disable paasd.service | Disabled (symlink removed) |

**Total downtime: ~15 seconds** (steps 6-10)

### Traefik Config Update

| Step | Action | Result |
|------|--------|--------|
| 12 | Copy updated traefik.yml | Done |
| 13 | Restart Traefik | Container restarted |

### Verification

| Step | Action | Result |
|------|--------|--------|
| 14 | ah.service status | `active (running)`, enabled |
| 15 | Service logs | Clean startup, no errors |
| 16 | Local health endpoint | `{"status":"ok"}` at `/v1/system/health` |
| 17 | Public website (https://agentic.hosting) | HTTP 200 |
| 18 | Public API health | `{"status":"ok"}` |
| 19 | Traefik container | `Up 50 seconds` |
| 20 | paasd disabled check | `disabled` |

## New Migrations Applied

- `state_011_service_dns_label.sql`
- `state_012_service_url.sql`

## Key Details

- **Binary**: `/usr/local/bin/ah`
- **Service**: `ah.service` (enabled, active)
- **Data dir**: `/var/lib/ah/`
- **Databases**: `/var/lib/ah/ah.db`, `/var/lib/ah/ah-metering.db`
- **Env file**: `/etc/default/ah`
- **Listen**: `0.0.0.0:8080`
- **Health endpoint**: `/v1/system/health` (not `/v1/health`)
- **Old service**: `paasd.service` (disabled, not removed)

## Notes

- The health endpoint is at `/v1/system/health`, not `/v1/health` as originally documented in the deployment plan.
- Dev mode warning is expected: `WARNING: running in dev mode -- HTTPS enforcement disabled`
- The `paasd.service` unit file was left in place (just disabled) as a rollback safety net.
