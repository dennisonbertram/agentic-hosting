# Session Log — 2026-03-30

## What Was Planned
- Deploy environments feature to production
- Tag v0.5.0 release
- Update build-log and documentation
- Expand test coverage for environments
- CI/CD decision document

## What Was Done
- Pre-flight checks: all 20 test packages pass, server healthy
- Discovery: environments feature was already deployed on March 24 (all 19 migrations applied)
- Fresh build deployed to confirm binary is current
- All 5 smoke tests pass (templates, CRUD, health)
- Warm pool active: 6 containers (2x default, 2x node, 2x python; no go warm pool)
- v0.5.0 tagged and released
- Build-log updated with migrations state_013-019, paasd references removed
- CI/CD decision doc written (recommends GitHub Actions + SSH Deploy)
- Test coverage expanded for environments API handlers and warm pool

## Key Discoveries
- Server was already running commit 2b49acf (deployed March 24)
- systemctl restart + pkill race condition: use stop/copy/start sequence instead
- tmpl_go has no warm pool containers — Go env creation will cold-start
- Bootstrap token uses X-Bootstrap-Token header (not Authorization: Bearer)
- gVisor release-20260302.0, Docker 29.3.0

## What Remains
- Implement CI/CD pipeline (decision doc done, implementation deferred)
- Add tmpl_go to warm pool config
- Fix flaky test: TestStartBuild_LogTruncationEndToEnd (SQLite contention)
- Fix deploy script race condition (pkill + systemctl restart)

## Blockers
None
